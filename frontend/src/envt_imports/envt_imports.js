// All API calls go through the Vite proxy (/api → http://localhost:5001)
// No environment variables needed on the frontend — the backend holds all secrets.
const envt_imports = {};

export default envt_imports;
